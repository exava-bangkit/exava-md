<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Exava" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Exava" parent="Base.Theme.Exava" />

    <style name="Theme.Exava.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#158a4d</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_white_center</item>
        <item name="postSplashScreenTheme">@style/Theme.Exava</item>
    </style>
</resources>